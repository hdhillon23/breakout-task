// Move right by increasing the x-coordinate
if(keyboard_check(ord("D")))
{
    x += 5; // Increase x to move right
}

// Move left by decreasing the x-coordinate
if(keyboard_check(ord("A")))
{
    x -= 5; // Decrease x to move left
}
